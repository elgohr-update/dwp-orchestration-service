groups:
- name: master
  jobs:
  - development
  - qa
  - integration
  - preprod
  - production
- name: pull-request
  jobs:
  - orchestration-service-pr
- name: roll-os-user
  jobs:
    - taint-dev
    - taint-qa
    - taint-int
    - taint-preprod
    - taint-prod
